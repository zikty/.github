name: PR title lint
on:
  pull_request:
    types: [opened, edited, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  lint:
    uses: zikty/zikty-ops/.github/workflows/reusable-pr-lint.yml@v1
    secrets: inherit
