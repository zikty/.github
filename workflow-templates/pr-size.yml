name: PR size
on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  size:
    uses: zikty/zikty-ops/.github/workflows/reusable-pr-size.yml@v1
    secrets: inherit
